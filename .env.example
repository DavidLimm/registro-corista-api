# Copie para .env (ignorado no Git) e ajuste os valores.
# Usado pelo docker compose e pela aplicação no perfil dev.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=registro_coristas
DB_USER=postgres
DB_PASSWORD=postgres
